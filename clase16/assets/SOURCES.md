Clase 16 ya no usa imágenes generadas para mostrar texto, tablas o diagramas: todo ese contenido
(comentarios del corpus, tokenización, bolsa de palabras, TF-IDF, probabilidades de Naive Bayes,
matrices de confusión, pipeline y código de Colab) está escrito directamente en `index.html` como
marcado real (tablas, listas, tarjetas con borde de color), con su estilo en `clase16.css`.

Esta carpeta de assets se deja vacía intencionalmente por ahora. Si en el futuro se agrega una
fotografía real (no un renderizado de texto/datos), documentar aquí su fuente y licencia.
